{
  "name": "chrome-custom-new-tab",
  "version": "1.3.8",
  "main": "index.html",
  "license": "ISC",
  "scripts": {
    "sass": "sass --watch --no-source-map ./",
    "build": "sass --no-source-map ./ && sh build.sh"
  },
  "devDependencies": {},
  "dependencies": {
    "sass": "^1.32.8"
  }
}
